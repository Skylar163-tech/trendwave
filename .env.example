# 本文件仅作变量名参考，可提交到仓库。
# 真实密钥请在「运营后台 → 模型」或「顶栏 → 集成配置」中填写，
# 或通过服务端环境变量注入；切勿把 API Key 写进 VITE_ 变量（会打进前端包）。

# 配置服务端口（默认 8787）
# TRENDWAVE_PORT=8787

# LLM 中转模式：服务端默认上游密钥（可选；也可在请求体 / 配置里带 apiKey）
# TRENDWAVE_LLM_KEY=
